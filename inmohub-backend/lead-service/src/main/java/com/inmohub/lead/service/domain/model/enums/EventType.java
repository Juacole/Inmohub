package com.inmohub.lead.service.domain.model.enums;

/**
 * Tipos de eventos registrables en la auditoria de leads:
 * CREATED (creacion), ASSIGNED (asignacion), STATUS_CHANGED (cambio de estado).
 */
public enum EventType {
    CREATED,
    ASSIGNED,
    STATUS_CHANGED
}
